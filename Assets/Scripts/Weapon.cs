using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Base class for the different weapons a ship can have
public class Weapon : MonoBehaviour
{
    public Ship mShip;
    public int minRange;
    public int maxRange;
    public int damage;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
